{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build main2",
            "command": "g++ -O3 -std=c++11 Shadow/main2.cpp -framework OpenGL -framework GLUT `pkg-config --cflags --libs opencv4` -Wno-deprecated -o Shadow/main2",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}